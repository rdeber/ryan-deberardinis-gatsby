<!-- AUTO-GENERATED-CONTENT:START (STARTER) -->
<p align="center">
  <a href="https://www.gatsbyjs.org">
    <img alt="Gatsby" src="https://www.gatsbyjs.org/monogram.svg" width="60" />
  </a>
</p>
<h1 align="center">
  Ryan DeBerardinis Gatsby Site
</h1>

## 🚀 Quick start

1.  **Install Gatsby**

    ```sh
    npm install --global gatsby-cli
    ```

2.  **Install Modules**

    Navigate into site’s directory and run:

    ```sh
    npm install
    ```

3.  **Start Gatsby**

    From inside the site’s directory run:

    ```sh
    gatsby develop
    ```

4.  **Open the source code and start editing!**

    Gatsby will start a hot-reloading development environment accessible by default at `http://localhost:8000`.

    \_Note: You'll also see a second link: `http://localhost:8000/___graphql`. This is a tool you can use to experiment with querying your data. Learn more about using this tool in the [Gatsby tutorial](https://www.gatsbyjs.org/tutorial/part-five/#introducing-graphiql).\_

5.  **Build site**

    From inside the site’s directory:

    ```sh
    gatsby build
    ```
    Gatsby will perform an optimized production build for your site, generating static HTML and per-route JavaScript code bundles.

6.  **Start Gatsby**

    From inside the site’s directory:

    ```sh
    gatsby serve
    ```
    Gatsby starts a local HTML server for testing your built site at `http://localhost:9000`